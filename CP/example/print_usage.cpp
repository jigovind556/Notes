#include <bits/stdc++.h>
#include "../debugging_codes/debugging.h"
using namespace std;

int main()
{
#ifndef ONLINE_JUDGE
    freopen("CP/compile/Error.txt", "w", stderr);
#endif
    int a = 2;
    int b = 3;
    vector<char> c = {'a', 'b', 'c'};
    vector<string> c1 = {"fdlk", "dlkaj", "dlsk"};
    set<string> c2 = {"fdlk", "dlkaj", "dlsk"};

    cin >> a;
    cout << MOD;
    debug(a);
    debug(b);

    debug(c);
    debug(c1);
    debug(c2);

    return 0;
}
